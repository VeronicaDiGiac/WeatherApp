import { RoundTempPipe } from './round-temp.pipe';

describe('RoundTempPipe', () => {
  it('create an instance', () => {
    const pipe = new RoundTempPipe();
    expect(pipe).toBeTruthy();
  });
});
